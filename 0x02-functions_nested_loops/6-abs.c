#include "main.h"

/**
 *_abs - Ebtey Point
 *@n: n is an integer
 *Description: Prints the absolute value of an integer
 *Return: Always 0.
 */

int _abs(int n)
{
if(n >= 0)
{
return (n);
}
else if (n < 0){
return (-n);
}
return (n);
}
