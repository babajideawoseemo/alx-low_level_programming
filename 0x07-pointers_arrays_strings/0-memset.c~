#include "main.h"

/**
 *_memset - The _memset() function fills the first
 * n bytes of the memory
 *@s:target
 *@b:constant byte
 *@n: number of byte
 *Return: returns new valu
 */

char *_memset(char *s, chr b, unsigned int n)
{
while (n)
{
s[n - 1] = b;
n--;
}
return (s);
}
