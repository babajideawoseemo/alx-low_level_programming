#include <stdio.h>

/**
*main -> assign a random number to the variable after execution
* and print the last digit of the number stored in variable n
*Return: Always 0 (Success)
*/

int main(void)
{
int number;
for(number = 0; number < 10; number++){
printf("%d", number);
}
printf("\n");
return (0);
}
