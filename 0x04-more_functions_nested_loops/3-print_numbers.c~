#include "main.h"

/**
*print_numbers - function that print all number from 0 to 9
*
*Return: returns void
*/
void print_numbers(void)
{
int num = 0;
do {
_putchar(num + 40);
num++;
} while (num >= 0 && num <= 9);
_putchar('\n');
}
